# PharmaTimer -- baseline dei reperti di lint. PUO SOLO SCENDERE.
# make lint arrossa solo se i reperti CRESCONO rispetto a questi numeri.
# Iscritta alla sessione di smontaggio del gate, su misura e non su stima.
#
# I numeri sono POST-CALIBRATURA. La prima passata dava 209 + 75 = 284, ma 103
# erano artefatti della configurazione e non del codice, e baselinarli avrebbe
# congelato rumore mio dentro il record:
#   - 43 no-undef per globals standard che avevo omesso (global, structuredClone,
#     HTMLElement, Element, getComputedStyle, Storage, ResizeObserver,
#     HTMLAnchorElement). Nessuno era un difetto del codice.
#   - 20 direttive eslint-disable segnalate solo perche avevo acceso
#     reportUnusedDisableDirectives con quelle regole non attive.
#   - 40 B008 di ruff: Depends() e Query() nei default degli argomenti sono IL
#     modo in cui FastAPI si scrive. Non sarebbero mai scesi.
#
# COSA RESTA, e cosa vale:
#   backend 169 -- 142 modernizzazioni di typing (UP006/UP045/UP035/UP042/UP017),
#                  22 ordinamenti di import (I001), e 5 IMPORT MORTI (F401).
#                  I cinque F401 sono i soli sostanziali: gli altri 164 sono
#                  stile, zero posta clinica.
#   frontend 20 -- 12 variabili non usate (no-unused-vars), reperti veri;
#                  8 riferimenti a regole di plugin non installati
#                  (react-hooks/exhaustive-deps 7, react/prop-types 1) citate da
#                  direttive inline nel codice. Questi 8 sono un difetto REALE
#                  del repo, non della config: sono direttive appese a plugin che
#                  nessuno ha mai configurato.
#
# Quando un reperto si chiude, si abbassa il numero qui. Mai alzarlo.
backend=169
frontend=20
